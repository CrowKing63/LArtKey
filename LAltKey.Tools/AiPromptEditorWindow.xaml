<Window x:Class="LAltKey.Tools.AiPromptEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI prompt editor"
        Width="760"
        Height="520"
        MinWidth="640"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SettingsBg}"
        KeyboardNavigation.TabNavigation="Cycle"
        AutomationProperties.Name="AI prompt editor window">
    <Window.Resources>
        <!-- text. -->
        <Style TargetType="Button" BasedOn="{StaticResource FluentButtonStyle}"/>
        <Style TargetType="TextBox" BasedOn="{StaticResource FluentTextBoxStyle}"/>
    </Window.Resources>

    <Border Margin="14"
            Padding="16"
            Background="{DynamicResource SettingsBg}"
            BorderBrush="{DynamicResource SettingsBorder}"
            BorderThickness="1"
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource FluentCardBorderStyle}" Margin="0,0,0,10">
                <TextBlock Margin="0"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Text="AI prompt editor"
                           AutomationProperties.Name="AI prompt editor title"/>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource FluentCardBorderStyle}" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Edit the default AI prompt below, then save."
                               TextWrapping="Wrap"
                               AutomationProperties.Name="AI prompt editor instructions"/>
                    <TextBlock Text="After saving, the main app reloads settings and uses the latest prompt."
                               Margin="0,4,0,0"
                               Foreground="{DynamicResource SettingsFgSub}"
                               TextWrapping="Wrap"
                               AutomationProperties.Name="AI prompt save notice"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource FluentCardBorderStyle}">
                <TextBox x:Name="PromptTextBox"
                         Text="{Binding PromptText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         TextWrapping="Wrap"
                         Padding="12,10"
                         FontSize="14"
                         KeyboardNavigation.TabIndex="0"
                         AutomationProperties.Name="AI default prompt input"/>
            </Border>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,12,0,0">
                <Button Content="Save"
                        Width="96"
                        Height="34"
                        Margin="0,0,8,0"
                        KeyboardNavigation.TabIndex="1"
                        Click="OnSave"
                        AutomationProperties.Name="Save AI prompt button"/>
                <Button Content="Close (Esc)"
                        Width="120"
                        Height="34"
                        KeyboardNavigation.TabIndex="2"
                        Click="OnClose"
                        AutomationProperties.Name="Close AI prompt button"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
