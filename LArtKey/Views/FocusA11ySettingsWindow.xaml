<Window x:Class="LArtKey.Views.FocusA11ySettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LArtKey.ViewModels"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="English text"
        Width="560" Height="460"
        MinWidth="500" MinHeight="400"
        WindowStartupLocation="Manual"
        Background="{DynamicResource SettingsBg}"
        d:DataContext="{d:DesignInstance vm:SettingsViewModel}">
    <Window.Resources>
        <!-- English text. -->
        <Style TargetType="Button" BasedOn="{StaticResource FluentButtonStyle}"/>
        <Style TargetType="TextBox" BasedOn="{StaticResource FluentTextBoxStyle}"/>
        <Style TargetType="ComboBox" BasedOn="{StaticResource FluentComboBoxStyle}"/>
    </Window.Resources>

    <Border Background="{DynamicResource SettingsBg}"
            CornerRadius="12"
            Padding="20">
        <StackPanel>
            <Border Style="{StaticResource FluentCardBorderStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="English text"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource SettingsFg}"
                               Margin="0,0,0,6"/>
                    <TextBlock Text="English text."
                               FontSize="11"
                               Foreground="{DynamicResource SettingsFgSub}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource FluentCardBorderStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock TextWrapping="Wrap"
                               FontSize="11"
                               Foreground="{DynamicResource SettingsFgSub}"
                               Margin="0,0,0,4"
                               Text="English text."/>
                    <TextBlock TextWrapping="Wrap"
                               FontSize="11"
                               Foreground="{DynamicResource SettingsFgSub}"
                               Margin="0,0,0,12"
                               Text="Tab, Shift+Tab, Enter, Space English text."/>

                    <TextBlock Text="English text" Foreground="{DynamicResource SettingsFgSub}" FontSize="11" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding KeyboardA11yNavigationScopes}"
                              SelectedItem="{Binding KeyboardA11yNavigationScope}"
                              Margin="0,0,0,8"/>

                    <TextBlock Text="English text (VK_English text)" Foreground="{DynamicResource SettingsFgSub}" FontSize="11" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding KeyboardA11yExitKey, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,12"/>

                    <TextBlock Text="LiveRegion English text" Foreground="{DynamicResource SettingsFgSub}" FontSize="11" Margin="0,0,0,4"/>
                    <CheckBox Content="English text"
                              Foreground="{DynamicResource SettingsFg}"
                              IsChecked="{Binding KeyboardA11yAnnounceFocus}"/>
                </StackPanel>
            </Border>

            <Button Content="English text"
                    Height="34"
                    HorizontalAlignment="Right"
                    Width="110"
                    Click="CloseButton_Click"
                    Background="{DynamicResource SettingsHighlight}"
                    Foreground="{DynamicResource SettingsFg}"
                    BorderThickness="0"
                    Cursor="Hand"
                    AutomationProperties.Name="English text"/>
        </StackPanel>
    </Border>
</Window>
